# start stream of data from json file at frequency of 0.5Hz
# the main server gets data, processes data stream, query the model for response, stores it in database.
# once the server recieves request good/bad weld, the model refreshes with this request data.